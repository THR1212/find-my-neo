# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# .env.local holds the Vercel OIDC token and, in live mode, the LLM API key.
# .env* is deliberately broad — .env.example is force-added and is the only one tracked.
.env*
!.env.example
.vercel

# Capture artifacts from walking Neo's live flows: screen recordings and HARs.
# Kept local on purpose — recordings show the tester's email and signup, and HARs
# contain session cookies and auth tokens. Findings belong in docs/neo-product-facts.md;
# the raw captures do not belong in the repo (they are also ~100MB).
neo_flows/
*.har
*.webm

# Bookmarklet output embeds the Vercel share token — keep it local. The token itself is
# NOT in source.js any more: build.mjs reads it from VERCEL_SHARE_TOKEN. It was hardcoded
# there until 03 Sep, which quietly defeated this rule — source.js is tracked, so the
# token was in the repo regardless of what the generated file did.
tools/bookmarklet/bookmarklet.txt
bm_payload.txt

# tsc -b writes this next to api/tsconfig.json. Build output, never source.
api/tsconfig.tsbuildinfo

# Raw source data — real customer records. Never commit.
analysis/data/
analysis/.venv/

# Per-run records written by the dev server. Contain business descriptions people typed.
runs.jsonl
